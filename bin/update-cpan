#!/bin/sh
action="${0##*/}"      # Basename
action="${action%%-*}" # Remove everything after first '-'

if [ -n "$1" ]; then
	pkg="$1"
else
	pkg="$(git branch --show-current)"
fi

ssh Alpine -t "cd "$PWD"/"$(find_repo "$pkg")" && apkbuild-cpan $action"
