#!/usr/bin/ion
let branches = $or(@args[1..] master)

for branch in @split($branches)
	env PRINTERR_QUIET=yes PRINTOK_QUIET=yes gbr $branch
	pullp
	env FORCE_PUSH=1 pushp remote:origin "$branch":"$branch"
end
