#!/bin/sh
[ -e .git ] || exit 
[ $# -lt 1 ] && set -- $(git branch --show-current)

for branch; do
    case "$branch" in
        3.10*) echo 3.10 ;;
        3.11*) echo 3.11 ;; 
        3.12*) echo 3.12 ;;
        3.13*) echo 3.13 ;;
    esac
done
