#!/usr/bin/ion
test $len(@args) -eq 1 && exit 1

if test $len(@args) -lt 3
	let args ++= @args[1]-$(random)
end

git switch --force-create @args[2] $or($TARGET_BRANCH, "master")
if not mr @args[1]
	printerr "Failed to apply '@args[1]'. Dropping to shell"
	${env::SHELL} -l
end
pullp
