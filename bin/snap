#!/bin/sh

CUR=${2:-$(pfw)}
ROOT=$(lsw -r)
SW=$(wattr w $ROOT)
SH=$(( $(wattr h $ROOT)))

BW=$(wattr b $CUR)
W=$(wattr w $CUR)
H=$(wattr h $CUR)

X=20
Y=20

case $1 in
	tl) Y=$((0 + BW*2 + 30)) ;;
    tr) X=$((SW - W - BW*2 - 20))
		Y=$((0 + BW*2 + 30)) ;;
    bl) Y=$((SH - H - BW*2 - 10)) ;;
    br) X=$((SW - W - BW*2 - 20))
        Y=$((SH - H - BW*2 - 10)) ;;
    md) X=$((SW/2 - W/2 - BW))
        Y=$((SH/2 - H/2 - BW));;
esac

wtp $X $Y $W $H $CUR
